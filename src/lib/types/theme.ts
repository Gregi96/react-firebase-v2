export type AppTheme = {
    colors: {
        backgroundColor: string,
        primary: string,
        foreground: string,
        error: string,
        typography: string,
        input: string
    }
}
